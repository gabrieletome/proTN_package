% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions_spatial.R
\name{moran_index}
\alias{moran_index}
\title{Parallel Moran's I Computation for Protein Spatial Data}
\usage{
moran_index(
  proteome_data,
  geneNames = NULL,
  nPixels = NULL,
  nsim = 999,
  impute = FALSE,
  cutoff = 8,
  ncores = NULL
)
}
\arguments{
\item{proteome_data}{A list containing proteome data, including gene intensity data and sample annotations.}

\item{geneNames}{A character vector of gene names to process.}

\item{nPixels}{Integer number of total pixels per gene.}

\item{nsim}{Number of Monte Carlo simulations to use for significance testing (default: 999).}

\item{impute}{Logical; if TRUE, imputes missing values using local means (3x3 kernel).}

\item{cutoff}{Minimum number of valid (non-NA) values required to compute Moran's I (default: 8).}

\item{ncores}{Numeric; Set the max available cores for multithreading (default: ncores-1)}
}
\value{
A \code{data.table} with columns:
\describe{
\item{gene}{Gene name.}
\item{moran}{Observed Moran's I statistic.}
\item{p.value}{Monte Carlo p-value.}
\item{valid.values}{Number of valid (non-NA) pixels.}
}
}
\description{
Computes Moran's I index of spatial autocorrelation for multiple proteins
in parallel. Supports multi-plate layouts, optional imputation of missing
values, and plotting of spatial maps.
}
\examples{
\dontrun{
results <- moran_index(protein.data, geneNames = c("TP53", "SMN1"), nPixels = 384)
}
}
